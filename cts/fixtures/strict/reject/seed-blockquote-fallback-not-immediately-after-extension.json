{
  "schemaVersion": "1",
  "id": "seed-blockquote-fallback-not-immediately-after-extension",
  "specVersion": "&ND Core v1",
  "mode": "strict",
  "source": "> Quote intro\n>\n> +++unsupported/extension\n> opaque payload\n> +++\n> Intervening paragraph\n>\n> +++fallback\n> Show fallback\n> +++\n",
  "expected": {
    "ok": false,
    "errorCode": "orphan_fallback_block",
    "assertions": [
      "parse failure",
      "a fallback block inside a blockquote is orphaned if any intervening nested block appears",
      "fallback adjacency is local to the immediately preceding extension block inside the same blockquote context"
    ]
  }
}
