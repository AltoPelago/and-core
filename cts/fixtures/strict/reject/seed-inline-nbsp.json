{
  "schemaVersion": "1",
  "id": "seed-inline-nbsp",
  "specVersion": "&ND Core v1",
  "mode": "strict",
  "source": "A[_]B\n",
  "expected": {
    "ok": false,
    "errorCode": "unknown_inline_type",
    "assertions": [
      "`[_]` is not assigned in Core v1",
      "strict parsers fail closed instead of materializing a non-breaking-space node"
    ]
  }
}
