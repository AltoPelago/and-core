{
  "schemaVersion": "1",
  "id": "seed-inline-reserved-admonition-tag",
  "specVersion": "&ND Core v1",
  "mode": "strict",
  "source": "[! warning]\n",
  "expected": {
    "ok": false,
    "errorCode": "unknown_inline_type",
    "assertions": [
      "`[! ...]` remains reserved in Core v1",
      "strict parsers fail closed instead of assigning admonition semantics"
    ]
  }
}
