{
  "schemaVersion": "1",
  "id": "seed-inline-reserved-reference-tag",
  "specVersion": "&ND Core v1",
  "mode": "strict",
  "source": "[~ mention]\n",
  "expected": {
    "ok": false,
    "errorCode": "unknown_inline_type",
    "assertions": [
      "`[~ ...]` remains reserved in Core v1",
      "strict parsers fail closed instead of assigning reference semantics"
    ]
  }
}
