{
  "schemaVersion": "1",
  "id": "seed-inline-reserved-todo-unchecked-marker",
  "specVersion": "&ND Core v1",
  "mode": "strict",
  "source": "[ ]\n",
  "expected": {
    "ok": false,
    "errorCode": "unknown_inline_type",
    "assertions": [
      "`[ ]` remains reserved in Core v1",
      "strict parsers fail closed instead of assigning task-list semantics"
    ]
  }
}
