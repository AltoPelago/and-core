{
  "schemaVersion": "1",
  "id": "seed-list-item-blockquote-table-horizontal-rule-instead-of-body-row",
  "specVersion": "&ND Core v1",
  "mode": "strict",
  "source": "- Parent\n\n  > | A | B |\n  > | --- | --- |\n  > ---\n",
  "expected": {
    "ok": false,
    "errorCode": "invalid_table_shape",
    "assertions": [
      "parse failure",
      "a doubly nested table inside a list-item-contained blockquote cannot terminate early and reinterpret a quoted horizontal rule as a sibling block",
      "the line after the recognized doubly nested separator row is still checked against table shape"
    ]
  }
}
