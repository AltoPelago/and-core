{
  "schemaVersion": "1",
  "id": "seed-list-item-table-horizontal-rule-instead-of-body-row",
  "specVersion": "&ND Core v1",
  "mode": "strict",
  "source": "- Parent\n\n  | A | B |\n  | --- | --- |\n  ---\n",
  "expected": {
    "ok": false,
    "errorCode": "invalid_table_shape",
    "assertions": [
      "parse failure",
      "a nested table inside a list item cannot terminate early and reinterpret a following horizontal rule as a sibling nested block",
      "the line after the recognized nested separator row is still checked against table shape"
    ]
  }
}
