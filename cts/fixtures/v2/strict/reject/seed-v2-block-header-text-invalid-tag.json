{
  "schemaVersion": "1",
  "id": "seed-v2-block-header-text-invalid-tag",
  "specVersion": "&ND Core v2",
  "mode": "strict",
  "source": "&ND v2\n\n===bad tag\npayload\n===\n",
  "expected": {
    "ok": false,
    "errorCode": "invalid_header_text_block",
    "assertions": [
      "v2 strict mode rejects malformed ===name opener tags",
      "header-text block tag, when present, must match identifier grammar"
    ]
  }
}
