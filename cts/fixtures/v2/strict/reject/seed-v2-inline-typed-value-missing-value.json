{
  "schemaVersion": "1",
  "id": "seed-v2-inline-typed-value-missing-value",
  "specVersion": "&ND Core v2",
  "mode": "strict",
  "source": "&ND v2\n\nUse [:date] now.\n",
  "expected": {
    "ok": false,
    "errorCode": "invalid_typed_value",
    "assertions": [
      "v2 strict mode rejects typed values without a payload",
      "typed value promotion requires an explicit value"
    ]
  }
}
